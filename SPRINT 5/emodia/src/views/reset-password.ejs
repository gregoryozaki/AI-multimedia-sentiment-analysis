<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redefinir Senha • Emodia</title>

    <link rel="stylesheet" href="/css/forgot-password.css">
</head>

<body class="forgot-body">

<header class="forgot-header">
    <a href="/login" class="back-btn">← Voltar</a>
</header>

<section class="forgot-container">
    <div class="forgot-card">

        <div class="forgot-icon">🔑</div>

        <h2 class="forgot-title">Redefinir Senha</h2>
        <p class="forgot-subtitle">
            Crie uma nova senha para sua conta
        </p>

        <% if (error) { %>
            <div style="
                background:#ffd4d4;
                color:#7a0000;
                padding:10px;
                border-radius:10px;
                margin-bottom:15px;
                text-align:center;">
                <%= error %>
            </div>
        <% } %>

        <form action="/reset-password/<%= token %>" method="POST" class="forgot-form">

            <label for="password">Nova senha</label>
            <input 
                type="password"
                name="password"
                id="password"
                placeholder="Digite sua nova senha"
                required
            >

            <label for="confirmPassword" style="margin-top:15px;">
                Confirmar senha
            </label>
            <input 
                type="password"
                name="confirmPassword"
                id="confirmPassword"
                placeholder="Repita a nova senha"
                required
            >

            <button type="submit" class="forgot-btn">
                Atualizar Senha
            </button>
        </form>

        <p class="login-text">
            Lembrou a senha?
            <a href="/login" class="login-link">Fazer login</a>
        </p>

    </div>
</section>

</body>
</html>
